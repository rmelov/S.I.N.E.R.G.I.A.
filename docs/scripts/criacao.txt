CREATE TABLE STATUS_PROJETO(
	ID_STATUS_PROJETO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_STATUS_PROJETO INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_STATUS_PROJETO NVARCHAR(255) UNIQUE NOT NULL,
    HIERARQUIA_STATUS_PROJETO INT UNIQUE NOT NULL
);

CREATE CLUSTERED INDEX IX_STATUS_PROJETO_COD ON STATUS_PROJETO(COD_STATUS_PROJETO);

CREATE TABLE STATUS_DISCENTE(
	ID_STATUS_DISCENTE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_STATUS_DISCENTE INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_STATUS_DISCENTE NVARCHAR(255) UNIQUE NOT NULL,
    HIERARQUIA_STATUS_DISCENTE INT UNIQUE NOT NULL
);

CREATE CLUSTERED INDEX IX_STATUS_DISCENTE_COD ON STATUS_DISCENTE(COD_STATUS_DISCENTE);


CREATE TABLE PROJETO(
	ID_PROJETO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_PROJETO INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_PROJETO NVARCHAR(255) NOT NULL,
    DESCRICAO_PROJETO NVARCHAR(3500) NOT NULL,
	DATA_INICIO DATE NOT NULL,
	PRAZO_CONCLUSAO DATE,
	SLUG_PROJETO VARCHAR(42) UNIQUE NOT NULL,
	ID_STATUS_PROJETO UNIQUEIDENTIFIER NOT NULL,
	ID_DISCENTE_CURSO_INSTITUICAO UNIQUEIDENTIFIER NOT NULL
	--ID_AVATAR UNIQUEIDENTIFIER NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_PROJETO_DESIGNACAO_DISCENTE ON PROJETO(DESIGNACAO_PROJETO, ID_DISCENTE_CURSO_INSTITUICAO);
CREATE CLUSTERED INDEX IX_PROJETO_COD ON PROJETO(COD_PROJETO);
CREATE INDEX IX_PROJETO_SLUG ON PROJETO(SLUG_PROJETO);

CREATE TABLE DISCENTE(
	ID_DISCENTE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_DISCENTE INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_DISCENTE NVARCHAR(255) NOT NULL,
	SLUG_DISCENTE VARCHAR(42) UNIQUE NOT NULL,
	ID_STATUS_DISCENTE UNIQUEIDENTIFIER NOT NULL
	--ID_AVATAR UNIQUEIDENTIFIER NULL
);

CREATE CLUSTERED INDEX IX_DISCENTE_COD ON DISCENTE(COD_DISCENTE);
CREATE INDEX IX_DISCENTE_SLUG ON DISCENTE(SLUG_DISCENTE);


CREATE TABLE INSTITUICAO(
	ID_INSTITUICAO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
	COD_INSTITUICAO INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_INSTITUICAO NVARCHAR(255) UNIQUE NOT NULL,
	ACRONIMO_INSTITUICAO VARCHAR(100) UNIQUE NOT NULL,
	RA_MASCARA VARCHAR(100),
	RA_QUANTIDADE_MIN_CARACTERES INT,
	RA_QUANTIDADE_MAX_CARACTERES INT
);

CREATE CLUSTERED INDEX IX_INSTITUICAO_COD ON INSTITUICAO(COD_INSTITUICAO);

CREATE TABLE CURSO(
	ID_CURSO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_CURSO INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_CURSO NVARCHAR(255) UNIQUE NOT NULL
);

CREATE CLUSTERED INDEX IX_CURSO_COD ON CURSO(COD_CURSO);


CREATE TABLE TELEFONE(
	ID_TELEFONE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_TELEFONE INT IDENTITY(1,1) UNIQUE NOT NULL,
	NUMERO_TELEFONE VARCHAR(20) UNIQUE NOT NULL
);

CREATE CLUSTERED INDEX IX_TELEFONE_COD ON TELEFONE(COD_TELEFONE);

CREATE TABLE DOMINIO_COMUM(
	ID_DOMINIO_COMUM UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_DOMINIO_COMUM INT IDENTITY(1,1) UNIQUE NOT NULL,
	SUFIXO_DOMINIO VARCHAR(255) UNIQUE NOT NULL
);

CREATE CLUSTERED INDEX IX_DOMINIO_COMUM_COD ON DOMINIO_COMUM(COD_DOMINIO_COMUM);

CREATE TABLE EMAIL(
	ID_EMAIL UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_EMAIL INT IDENTITY(1,1) UNIQUE NOT NULL,
	ENDERECO_EMAIL VARCHAR(255) NOT NULL,
	ID_DOMINIO_COMUM UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_EMAIL_DOMINIO_COMUM ON EMAIL(ENDERECO_EMAIL, ID_DOMINIO_COMUM);
CREATE CLUSTERED INDEX IX_EMAIL_COD ON EMAIL(COD_EMAIL);

CREATE TABLE DOMINIO_INSTITUICAO(
    ID_DOMINIO_INSTITUICAO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
	COD_DOMINIO_INSTITUICAO INT IDENTITY(1,1) UNIQUE NOT NULL,
    SUFIXO_DOMINIO VARCHAR(255) UNIQUE NOT NULL,
	ID_INSTITUICAO UNIQUEIDENTIFIER NOT NULL
);

CREATE CLUSTERED INDEX IX_DOMINIO_INSTITUICAO_COD ON DOMINIO_INSTITUICAO(COD_DOMINIO_INSTITUICAO);

CREATE TABLE EMAIL_INSTITUCIONAL(
	ID_EMAIL_INSTITUCIONAL UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_EMAIL_INSTITUCIONAL INT IDENTITY(1,1) UNIQUE NOT NULL,
	ENDERECO_EMAIL_INSTITUCIONAL VARCHAR(255) UNIQUE NOT NULL,
	ID_DOMINIO_INSTITUICAO UNIQUEIDENTIFIER NOT NULL
);

CREATE CLUSTERED INDEX IX_EMAIL_INSTITUCIONAL_COD ON EMAIL_INSTITUCIONAL(COD_EMAIL_INSTITUCIONAL);

CREATE TABLE REDE(
	ID_REDE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_REDE INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_REDE NVARCHAR(255) UNIQUE NOT NULL,
	NOME_DOMINIO_REDE VARCHAR(255) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL
);

CREATE CLUSTERED INDEX IX_REDE_COD ON REDE(COD_REDE);



CREATE TABLE HABILIDADE(
	ID_HABILIDADE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_HABILIDADE INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_HABILIDADE NVARCHAR(255) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL
);

CREATE CLUSTERED INDEX IX_HABILIDADE_COD ON HABILIDADE(COD_HABILIDADE);

CREATE TABLE NIVEL_HABILIDADE(
	ID_NIVEL_HABILIDADE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_NIVEL_HABILIDADE INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_NIVEL NVARCHAR(255) UNIQUE,
    HIERARQUIA_NIVEL INT UNIQUE NOT NULL
);

CREATE CLUSTERED INDEX IX_NIVEL_HABILIDADE_COD ON NIVEL_HABILIDADE(COD_NIVEL_HABILIDADE);

CREATE TABLE CLASSIFICACAO(
	ID_CLASSIFICACAO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_CLASSIFICACAO INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_CLASSIFICACAO NVARCHAR(255) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL
);

CREATE CLUSTERED INDEX IX_CLASSIFICACAO_COD ON CLASSIFICACAO(COD_CLASSIFICACAO);

CREATE TABLE AREA_ATUACAO(
	ID_AREA_ATUACAO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_AREA_ATUACAO INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_AREA_ATUACAO NVARCHAR(255) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL
);

CREATE CLUSTERED INDEX IX_AREA_ATUACAO_COD ON AREA_ATUACAO(COD_AREA_ATUACAO);

CREATE TABLE ORDEM_AUTORIA(
	ID_ORDEM_AUTORIA UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_ORDEM_AUTORIA INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_ORDEM_AUTORIA NVARCHAR(255) UNIQUE NOT NULL,
    HIERARQUIA_ORDEM_AUTORIA INT UNIQUE NOT NULL
);

CREATE CLUSTERED INDEX IX_ORDEM_AUTORIA_COD ON ORDEM_AUTORIA(COD_ORDEM_AUTORIA);


CREATE TABLE TELEFONE_PROJETO(
	ID_TELEFONE_PROJETO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_TELEFONE_PROJETO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_PROJETO UNIQUEIDENTIFIER NOT NULL,
	ID_TELEFONE UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_TELEFONE_PROJETO ON TELEFONE_PROJETO(ID_PROJETO, ID_TELEFONE);
CREATE CLUSTERED INDEX IX_TELEFONE_PROJETO_COD ON TELEFONE_PROJETO(COD_TELEFONE_PROJETO);

CREATE TABLE EMAIL_PROJETO(
	ID_EMAIL_PROJETO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_EMAIL_PROJETO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_PROJETO UNIQUEIDENTIFIER NOT NULL,
	ID_EMAIL UNIQUEIDENTIFIER NULL,
    ID_EMAIL_INSTITUCIONAL UNIQUEIDENTIFIER NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_EMAIL_PROJETO ON EMAIL_PROJETO(ID_PROJETO, ID_EMAIL) WHERE ID_EMAIL IS NOT NULL;
CREATE UNIQUE NONCLUSTERED INDEX UIX_EMAIL_INSTITUCIONAL_PROJETO_INST ON EMAIL_PROJETO(ID_PROJETO, ID_EMAIL_INSTITUCIONAL) WHERE ID_EMAIL_INSTITUCIONAL IS NOT NULL;

CREATE CLUSTERED INDEX IX_EMAIL_PROJETO_COD ON EMAIL_PROJETO(COD_EMAIL_PROJETO);

CREATE TABLE REDE_PROJETO(
	ID_REDE_PROJETO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_REDE_PROJETO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_PROJETO UNIQUEIDENTIFIER NOT NULL,
	ID_REDE UNIQUEIDENTIFIER NOT NULL,
	URL_REDE VARCHAR(255) NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_REDE_PROJETO ON REDE_PROJETO(ID_PROJETO, URL_REDE);
CREATE CLUSTERED INDEX IX_REDE_PROJETO_COD ON REDE_PROJETO(COD_REDE_PROJETO);

CREATE TABLE TELEFONE_DISCENTE(
	ID_TELEFONE_DISCENTE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_TELEFONE_DISCENTE INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL,
	ID_TELEFONE UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_TELEFONE_DISCENTE ON TELEFONE_DISCENTE(ID_DISCENTE, ID_TELEFONE);
CREATE CLUSTERED INDEX IX_TELEFONE_DISCENTE_COD ON TELEFONE_DISCENTE(COD_TELEFONE_DISCENTE);

CREATE TABLE EMAIL_DISCENTE(
	ID_EMAIL_DISCENTE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_EMAIL_DISCENTE INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL,
	ID_EMAIL UNIQUEIDENTIFIER UNIQUE NOT NULL
);

CREATE CLUSTERED INDEX IX_EMAIL_DISCENTE_COD ON EMAIL_DISCENTE(COD_EMAIL_DISCENTE);

CREATE TABLE EMAIL_INSTITUCIONAL_DISCENTE(
	ID_EMAIL_INSTITUCIONAL_DISCENTE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_EMAIL_INSTITUCIONAL_DISCENTE INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL,
	ID_EMAIL_INSTITUCIONAL UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_EMAIL_INSTITUCIONAL_DISCENTE ON EMAIL_INSTITUCIONAL_DISCENTE(ID_DISCENTE, ID_EMAIL_INSTITUCIONAL);
CREATE CLUSTERED INDEX IX_EMAIL_INSTITUCIONAL_DISCENTE_COD ON EMAIL_INSTITUCIONAL_DISCENTE(COD_EMAIL_INSTITUCIONAL_DISCENTE);

CREATE TABLE REDE_DISCENTE(
	ID_REDE_DISCENTE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_REDE_DISCENTE INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL,
	ID_REDE UNIQUEIDENTIFIER NOT NULL,
	URL_REDE VARCHAR(255) UNIQUE NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_REDE_DISCENTE ON REDE_DISCENTE(ID_DISCENTE, URL_REDE);
CREATE CLUSTERED INDEX IX_REDE_DISCENTE_COD ON REDE_DISCENTE(COD_REDE_DISCENTE);


CREATE TABLE CURSO_INSTITUICAO(
	ID_CURSO_INSTITUICAO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_CURSO_INSTITUICAO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_CURSO UNIQUEIDENTIFIER NOT NULL,
	ID_INSTITUICAO UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_CURSO_INSTITUICAO ON CURSO_INSTITUICAO(ID_CURSO, ID_INSTITUICAO);
CREATE CLUSTERED INDEX IX_CURSO_INSTITUICAO_COD ON CURSO_INSTITUICAO(COD_CURSO_INSTITUICAO);

CREATE TABLE DISCENTE_CURSO_INSTITUICAO(
	ID_DISCENTE_CURSO_INSTITUICAO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_DISCENTE_CURSO_INSTITUICAO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL,
	ID_CURSO_INSTITUICAO UNIQUEIDENTIFIER NOT NULL,
    RA_DISCENTE VARCHAR(100) NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_DISCENTE_CURSO_INSTITUICAO_ID ON DISCENTE_CURSO_INSTITUICAO(ID_DISCENTE, ID_CURSO_INSTITUICAO);
CREATE UNIQUE NONCLUSTERED INDEX UIX_DISCENTE_CURSO_INSTITUICAO_RA ON DISCENTE_CURSO_INSTITUICAO(RA_DISCENTE, ID_CURSO_INSTITUICAO);
CREATE CLUSTERED INDEX IX_DISCENTE_CURSO_INSTITUICAO_COD ON DISCENTE_CURSO_INSTITUICAO(COD_DISCENTE_CURSO_INSTITUICAO);


CREATE TABLE HABILIDADE_CURSO(
	ID_HABILIDADE_CURSO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_HABILIDADE_CURSO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_HABILIDADE UNIQUEIDENTIFIER NOT NULL,
	ID_CURSO UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_HABILIDADE_CURSO ON HABILIDADE_CURSO(ID_HABILIDADE, ID_CURSO);
CREATE CLUSTERED INDEX IX_HABILIDADE_CURSO_COD ON HABILIDADE_CURSO(COD_HABILIDADE_CURSO);


CREATE TABLE DISCENTE_HABILIDADE(
	ID_DISCENTE_HABILIDADE UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_DISCENTE_HABILIDADE INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_DISCENTE UNIQUEIDENTIFIER NOT NULL,
	ID_HABILIDADE UNIQUEIDENTIFIER NOT NULL,
	ID_NIVEL_HABILIDADE UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_DISCENTE_HABILIDADE ON DISCENTE_HABILIDADE(ID_DISCENTE, ID_HABILIDADE);
CREATE CLUSTERED INDEX IX_DISCENTE_HABILIDADE_COD ON DISCENTE_HABILIDADE(COD_DISCENTE_HABILIDADE);

CREATE TABLE CURSO_REQUISITADO(
	ID_CURSO_REQUISITADO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_CURSO_REQUISITADO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_PROJETO UNIQUEIDENTIFIER NOT NULL,
	ID_CURSO UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_CURSO_REQUISITADO ON CURSO_REQUISITADO(ID_PROJETO, ID_CURSO);
CREATE CLUSTERED INDEX IX_CURSO_REQUISITADO_COD ON CURSO_REQUISITADO(COD_CURSO_REQUISITADO);

CREATE TABLE HABILIDADE_REQUISITADA(
	ID_HABILIDADE_REQUISITADA UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_HABILIDADE_REQUISITADA INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_PROJETO UNIQUEIDENTIFIER NOT NULL,
	ID_HABILIDADE UNIQUEIDENTIFIER NOT NULL,
	ID_NIVEL_HABILIDADE UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_HABILIDADE_REQUISITADA ON HABILIDADE_REQUISITADA(ID_PROJETO, ID_HABILIDADE);
CREATE CLUSTERED INDEX IX_HABILIDADE_REQUISITADA_COD ON HABILIDADE_REQUISITADA(COD_HABILIDADE_REQUISITADA);

CREATE TABLE AUTORIA_PROJETO(
	ID_AUTORIA_PROJETO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_AUTORIA_PROJETO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_PROJETO UNIQUEIDENTIFIER NOT NULL,
	ID_DISCENTE_CURSO_INSTITUICAO UNIQUEIDENTIFIER NOT NULL,
	ID_ORDEM_AUTORIA UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_AUTORIA_PROJETO ON AUTORIA_PROJETO(ID_PROJETO, ID_DISCENTE_CURSO_INSTITUICAO, ID_ORDEM_AUTORIA);
CREATE CLUSTERED INDEX IX_AUTORIA_PROJETO_COD ON AUTORIA_PROJETO(COD_AUTORIA_PROJETO);

CREATE TABLE CLASSIFICACAO_PROJETO(
	ID_CLASSIFICACAO_PROJETO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_CLASSIFICACAO_PROJETO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_PROJETO UNIQUEIDENTIFIER NOT NULL,
	ID_CLASSIFICACAO UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_CLASSIFICACAO_PROJETO ON CLASSIFICACAO_PROJETO(ID_PROJETO, ID_CLASSIFICACAO);
CREATE CLUSTERED INDEX IX_CLASSIFICACAO_PROJETO_COD ON CLASSIFICACAO_PROJETO(COD_CLASSIFICACAO_PROJETO);

CREATE TABLE AREA_ATUACAO_PROJETO(
	ID_AREA_ATUACAO_PROJETO UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_AREA_ATUACAO_PROJETO INT IDENTITY(1,1) UNIQUE NOT NULL,
	ID_PROJETO UNIQUEIDENTIFIER NOT NULL,
	ID_AREA_ATUACAO UNIQUEIDENTIFIER NOT NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_AREA_ATUACAO_PROJETO ON AREA_ATUACAO_PROJETO(ID_PROJETO, ID_AREA_ATUACAO);
CREATE CLUSTERED INDEX IX_AREA_ATUACAO_PROJETO_COD ON AREA_ATUACAO_PROJETO(COD_AREA_ATUACAO_PROJETO);


CREATE TABLE PARAMETRO_HIERARQUIA(
	ID_PARAMETRO_HIERARQUIA UNIQUEIDENTIFIER PRIMARY KEY NONCLUSTERED DEFAULT NEWID() NOT NULL,
    COD_PARAMETRO_HIERARQUIA INT IDENTITY(1,1) UNIQUE NOT NULL,
	DESIGNACAO_PARAMETRO NVARCHAR(255) UNIQUE NOT NULL,
	DESCRICAO_PARAMETRO NVARCHAR(500) UNIQUE NOT NULL,
    ID_STATUS_PROJETO UNIQUEIDENTIFIER NULL,
    ID_STATUS_DISCENTE UNIQUEIDENTIFIER NULL,
    ID_ORDEM_AUTORIA UNIQUEIDENTIFIER NULL
);

CREATE UNIQUE NONCLUSTERED INDEX UIX_PARAMETRO_STATUS_PROJETO ON PARAMETRO_HIERARQUIA(ID_STATUS_PROJETO) WHERE ID_STATUS_PROJETO IS NOT NULL;
CREATE UNIQUE NONCLUSTERED INDEX UIX_PARAMETRO_STATUS_DISCENTE ON PARAMETRO_HIERARQUIA(ID_STATUS_DISCENTE) WHERE ID_STATUS_DISCENTE IS NOT NULL;
CREATE UNIQUE NONCLUSTERED INDEX UIX_PARAMETRO_ORDEM_AUTORIA ON PARAMETRO_HIERARQUIA(ID_ORDEM_AUTORIA) WHERE ID_ORDEM_AUTORIA IS NOT NULL;

CREATE CLUSTERED INDEX IX_PARAMETRO_HIERARQUIA_COD ON PARAMETRO_HIERARQUIA(COD_PARAMETRO_HIERARQUIA);